
div(ng-app='ng-app')
  h2 Todo
  div(ng-controller='ChannelCtrl')

    form(class='form-inline', ng-submit='joinChannel()')
      input(type="text", ng-model="channelName", 
        placeholder="Enter channel name")

      input.btn.btn-primary(type='submit', value='Join')

    
    span {{messages.length}} items in messages
